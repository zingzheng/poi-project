create table JobInfo (
  id integer primary key autoincrement,
  core_type,
  map_type,
  region_type,
  region,
  keyword,
  delta,
  nex, 
  boxs,
  res,
  log,
  owner,
  createTime,
  finishTime,
  status
);